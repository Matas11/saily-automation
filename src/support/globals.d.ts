interface Window {
  __SB_INIT__: {
    config: {
      fr: number;
      lang: string;
      country: string;
      initializible: boolean;
      tz: string;
      pre_path: string;
      live_path: string;
      b_history: string;
      format: string;
      preloader: boolean;
      load_history: boolean;
      token: string | null;
      debug_mode: boolean;
    };
  };
}